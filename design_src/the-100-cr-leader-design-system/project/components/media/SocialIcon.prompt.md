`SocialIcon` is the circular, outlined social link used in the "Circle" footer block; it fills with sand on hover.

```jsx
<SocialIcon platform="linkedin" href="#" />
<SocialIcon platform="x" tone="dark" size={48} />
```

Platforms: `linkedin`, `facebook`, `x`, `instagram`, `whatsapp`. `tone="dark"` for forest sections.
