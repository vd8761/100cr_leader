`PlatformIcon` is the circular brand badge for a listening platform.

```jsx
<PlatformIcon platform="spotify" size={48} />
<PlatformIcon platform="google" size={40} ring />
```

Platforms: `spotify`, `youtube`, `google` (multicolor Google Podcasts on white), `jiosaavn`. Use `ring` for the white Google badge on light surfaces.
