`NavBar` is the minimal centered podcast nav.

```jsx
<NavBar items={['Home','Podcast','Connect']} active="Home" tone="dark" />
```

Centered links; active item is full-strength, others muted. `tone="dark"` for the forest hero.
