`Marquee` is the scrolling host-name strip under the hero headline - "Bharathiraja Thangapplalam" repeated with mic glyphs between.

```jsx
<Marquee text="Bharathiraja Thangapplalam" icon="mic" tone="dark" speed={30} />
```

`speed` is seconds per loop (lower = faster). `tone="dark"` on forest.
