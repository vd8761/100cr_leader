`Badge` is the small pill label - most visibly the "EP 1" episode tag.

```jsx
<Badge tone="forest">EP 1</Badge>
<Badge tone="gold">New</Badge>
```

Tones: `forest` (dark pill / sand text - default), `sand`, `outline`, `gold`.
